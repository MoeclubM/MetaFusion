name: Release & Container Publishing

on:
  push:
    branches: [main]
    tags: ["v*.*.*", "v*"]
  workflow_dispatch:
    inputs:
      publish_latest:
        description: "是否同时打上 latest 标签"
        required: true
        default: true
        type: boolean

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  docker-publish:
    name: Build & Publish Container Images
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: backend-api
            context: ./backend
            dockerfile: ./backend/Dockerfile
            target: server
            image: ghcr.io/${{ github.repository }}/backend
          - name: backend-worker
            context: ./backend
            dockerfile: ./backend/Dockerfile
            target: worker
            image: ghcr.io/${{ github.repository }}/worker
          - name: backend-migrator
            context: ./backend
            dockerfile: ./backend/Dockerfile
            target: migrator
            image: ghcr.io/${{ github.repository }}/migrator
          - name: frontend-app
            context: ./frontend
            dockerfile: ./frontend/Dockerfile
            target: ""
            image: ghcr.io/${{ github.repository }}/frontend
          - name: docs-site
            context: ./docs-site
            dockerfile: ./docs-site/Dockerfile
            target: ""
            image: ghcr.io/${{ github.repository }}/docs-site

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up QEMU (Multi-platform emulation)
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (Tags & Labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ matrix.image }}
          flavor: |
            latest=auto
          tags: |
            type=ref,event=tag
            type=ref,event=branch
            type=sha,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' || github.event.inputs.publish_latest == 'true' }}

      - name: Build and push container image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          target: ${{ matrix.target != '' && matrix.target || null }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}

  github-release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [docker-publish]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build Cross-Platform Migration Tools
        working-directory: backend
        run: |
          mkdir -p ../dist
          GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-w -s" -o ../dist/mf-migrate-linux-amd64 ./cmd/migrate
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -ldflags="-w -s" -o ../dist/mf-migrate-linux-arm64 ./cmd/migrate
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -ldflags="-w -s" -o ../dist/mf-migrate-windows-amd64.exe ./cmd/migrate
          GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -ldflags="-w -s" -o ../dist/mf-migrate-darwin-arm64 ./cmd/migrate

      - name: Generate Changelog & Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          prerelease: false
          generate_release_notes: true
          files: |
            dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
